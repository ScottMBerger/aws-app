{
    "projectName": "amplify",
    "version": "2.0",
    "frontend": "javascript",
    "javascript": {
        "framework": "angular",
        "config": {
            "SourceDir": "src",
            "DistributionDir": "dist",
            "BuildCommand": "ng build --prod && ng run aws-app:server:production",
            "StartCommand": "ng serve"
        }
    },
    "providers": [
        "awscloudformation"
    ]
}